name: CI
on:
  push:
    branches: ["**"] # Every push to every branch
  pull_request:
    branches: ["**"] # Every PR on every branch

jobs:
  test:
    uses: ./.github/workflows/reusable-tests.yml # Call the source-of-truth tests
